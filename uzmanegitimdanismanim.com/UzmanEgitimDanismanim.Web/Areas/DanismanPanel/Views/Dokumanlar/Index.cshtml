@using UzmanEgitimDanismanim.Shared.Dtos
@model List<OgrenciDokumanDto>

@{
    ViewData["Title"] = "Danışman Panel - Dokumanlar";
}


<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Dokümanlar</h1>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>



@if (Model.Count>0)
{
<div class="card card-danger">
    <div class="card-header">
        <h3 class="card-title">Dokümanlar</h3>

        <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse">
                <i class="fas fa-minus"></i>
            </button>
            <button type="button" class="btn btn-tool" data-card-widget="remove">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
    <div class="card-body" id="dersGrafikChartContent">

        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>Doküman Adı</th>
                    <th>Danışman Raporu</th>
                    <th>Ekleme Tarihi</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="gradeX" data-widget="expandable-table" aria-expanded="false">
                        <td>@Html.DisplayFor(modelItem => item.DokumanAdi)</td>

                        <td>
                            <a href="~/Panel/Files/Dokuman/@Html.DisplayFor(modelItem => item.Dokuman)" target="_blank">@item.Dokuman</a>
                        </td>
                        <td>@item.IslemTarihi.ToString("dd MMMM yyyy")</td>
                    </tr>
                }
            </tbody>
        </table>


    </div>
    <!-- /.card-body -->
</div>
<!-- /.card -->
}